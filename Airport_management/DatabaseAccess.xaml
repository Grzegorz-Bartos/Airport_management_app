<Page x:Class="Airport_management.DatabaseAccess"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Airport_management"
      mc:Ignorable="d"
      Height="360" Width="600"
      Title="DatabaseAccess">

    <Grid>
        <StackPanel>
            <Label x:Name="LB_database" Content="Enter database password: " Style="{DynamicResource Label1}" Margin="0 60 0 0"/>
            <PasswordBox x:Name="PB_database" Style="{DynamicResource PasswordBox1}" Width="260"/>
            <Button x:Name="BT_confirm" Content="Confirm" Click="Confirm" Style="{DynamicResource Button1}" Width="260"/>
        </StackPanel>
    </Grid>
</Page>
